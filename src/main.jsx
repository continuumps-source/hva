import React from "react";
import ReactDOM from "react-dom/client";
import HVAReport from "./HVAReport.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HVAReport />
  </React.StrictMode>
);
